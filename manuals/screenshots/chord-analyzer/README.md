# Chord Analyzer screenshots

The DAW setup walkthroughs in the chapter are text-only by design (per-host instructions are clearer as steps than as screenshots). Only two shots needed.

| Filename | What it shows |
| --- | --- |
| `01-main-ui.png` | Full visualizer with a chord detected (e.g., a C major triad held on a controller). Chord name, suggestions, and inversion display all visible. |
| `02-suggestions-all.png` | Suggestion Level set to "All (+ Advanced)" with the suggestion tiers visible (basic, intermediate, advanced/substitution options). |
